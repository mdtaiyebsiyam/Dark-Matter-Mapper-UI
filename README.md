
  # Dark Matter Mapper UI

  This is a code bundle for Dark Matter Mapper UI. The original project is available at https://www.figma.com/design/DJSNIYYT2bCVRd64HmFrtz/Dark-Matter-Mapper-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  